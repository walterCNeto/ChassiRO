# Postgres (usado pelo script de export quando rodando local)
PGHOST=localhost
PGPORT=5432
PGUSER=chassi
PGPASSWORD=chassi
PGDATABASE=chassi

# Alternativamente, use uma DSN unica (tem precedencia):
# DATABASE_URL=postgresql://chassi:chassi@localhost:5432/chassi
